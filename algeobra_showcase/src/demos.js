"use strict";

/**
 * 
 * @param {HTMLCanvasElement} canvas 
 */
function demo0(canvas) {
    console.log(canvas.parentElement.getBoundingClientRect());

    const alg = algeobra;
    console.log(algeobra);

    const scene = new alg.GeometryScene();

    const diagram = new alg.

}

function test() {
    console.log("test1");
}
console.log("test");


// TODO remove, only for editing, as devserver doesn't work correctly

window["algeobra_showcase_demo_finished_loading"] = true;